CREATE TABLE  "CURSO"
   (	"ID_CURSO" NUMBER NOT NULL ENABLE,
	"NOME_CURSO" VARCHAR2(255) NOT NULL ENABLE,
	"GRAU_INSTRUCAO_CURSO" VARCHAR2(50) NOT NULL ENABLE,
	"FINALIDADE" VARCHAR2(150),
	"PARCERIA" VARCHAR2(150),
	"CARGA_HORARIA" NUMBER NOT NULL ENABLE,
	 CONSTRAINT "CURSO_PK" PRIMARY KEY ("ID_CURSO") ENABLE
   )
/


CREATE OR REPLACE TRIGGER  "BI_CURSO"
  before insert on "CURSO"               
  for each row  
begin   
    select "CURSO_SEQ".nextval into :NEW.ID_CURSO from dual;
end;

/
ALTER TRIGGER  "BI_CURSO" ENABLE
/


CREATE TABLE  "VINCULO"
   (	"ID_VINCULO" NUMBER NOT NULL ENABLE,
	"REGIME_JURIDICO" VARCHAR2(50) NOT NULL ENABLE,
	"TIPO" VARCHAR2(100),
	 CONSTRAINT "VINCULO_PK" PRIMARY KEY ("ID_VINCULO") ENABLE
   )
/


CREATE OR REPLACE TRIGGER  "BI_VINCULO"
  before insert on "VINCULO"               
  for each row  
begin   
    select "VINCULO_SEQ".nextval into :NEW.ID_VINCULO from dual;
end;

/
ALTER TRIGGER  "BI_VINCULO" ENABLE
/


CREATE TABLE  "CARGO"
   (	"ID_CARGO" NUMBER NOT NULL ENABLE,
	"NOME_CARGO" VARCHAR2(255) NOT NULL ENABLE,
	"NIVEL" VARCHAR2(50) NOT NULL ENABLE,
	"ESPECIALIZACAO" VARCHAR2(255),
	 CONSTRAINT "CARGO_PK" PRIMARY KEY ("ID_CARGO") ENABLE
   )
/


CREATE OR REPLACE TRIGGER  "BI_CARGO"
  before insert on "CARGO"               
  for each row  
begin   
    select "CARGO_SEQ".nextval into :NEW.ID_CARGO from dual;
end;

/
ALTER TRIGGER  "BI_CARGO" ENABLE
/


CREATE TABLE  "SERVIDOR"
   (	"ID_SERVIDOR" NUMBER NOT NULL ENABLE,
	"NOME_SERVIDOR" VARCHAR2(255) NOT NULL ENABLE,
	"RF_SERVIDOR" CHAR(8),
	"SEXO_SERVIDOR" CHAR(1) NOT NULL ENABLE,
	"CPF_SERVIDOR" CHAR(11),
	"RG_SERVIDOR" CHAR(8),
	"RACA_SERVIDOR" VARCHAR2(50) NOT NULL ENABLE,
	"DATA_NASC_SERVIDOR" DATE NOT NULL ENABLE,
	"RUA_SERVIDOR" VARCHAR2(150) NOT NULL ENABLE,
	"NUMERO-SERVIDOR" NUMBER NOT NULL ENABLE,
	"BAIRRO_SERVIDOR" VARCHAR2(150) NOT NULL ENABLE,
	"CEP_SERVIDOR" CHAR(8) NOT NULL ENABLE,
	"CIDADE_SERVIDOR" VARCHAR2(150) NOT NULL ENABLE,
	"ID_VINCULO" NUMBER,
	"ID_CARGO" NUMBER,
	 CONSTRAINT "SERVIDOR_PK" PRIMARY KEY ("ID_SERVIDOR") ENABLE,
	 CONSTRAINT "SERVIDOR_CK1" CHECK (SEXO_SERVIDOR IN ('M','F')) ENABLE,
	 CONSTRAINT "SERVIDOR_FK" FOREIGN KEY ("ID_VINCULO")
	  REFERENCES  "VINCULO" ("ID_VINCULO") ENABLE,
	 CONSTRAINT "SERVIDOR_FK2" FOREIGN KEY ("ID_CARGO")
	  REFERENCES  "CARGO" ("ID_CARGO") ENABLE
   )
/


CREATE OR REPLACE TRIGGER  "BI_SERVIDOR"
  before insert on "SERVIDOR"               
  for each row  
begin   
    select "SERVIDOR_SEQ".nextval into :NEW.ID_SERVIDOR from dual;
end;

/
ALTER TRIGGER  "BI_SERVIDOR" ENABLE
/


CREATE TABLE  "SERVIDOR_EMAIL"
   (	"ID_SERVIDOR_EMAIL" NUMBER NOT NULL ENABLE,
	"EMAIL_SERVIDOR" VARCHAR2(100),
	"ID_SERVIDOR" NUMBER,
	 CONSTRAINT "SERVIDOR_EMAIL_PK" PRIMARY KEY ("ID_SERVIDOR_EMAIL") ENABLE,
	 CONSTRAINT "SERVIDOR_EMAIL_FK" FOREIGN KEY ("ID_SERVIDOR")
	  REFERENCES  "SERVIDOR" ("ID_SERVIDOR") ENABLE
   )
/


CREATE OR REPLACE TRIGGER  "BI_SERVIDOR_EMAIL"
  before insert on "SERVIDOR_EMAIL"               
  for each row  
begin   
    select "SERVIDOR_EMAIL_SEQ".nextval into :NEW.ID_SERVIDOR_EMAIL from dual;
end;

/
ALTER TRIGGER  "BI_SERVIDOR_EMAIL" ENABLE
/


CREATE TABLE  "SERVIDOR_TELEFONE"
   (	"ID_SERVIDOR_TELEFONE" NUMBER NOT NULL ENABLE,
	"TELEFONE_SERVIDOR" VARCHAR2(50),
	"ID_SERVIDOR" NUMBER,
	 CONSTRAINT "SERVIDOR_TELEFONE_PK" PRIMARY KEY ("ID_SERVIDOR_TELEFONE") ENABLE,
	 CONSTRAINT "SERVIDOR_TELEFONE_FK" FOREIGN KEY ("ID_SERVIDOR")
	  REFERENCES  "SERVIDOR" ("ID_SERVIDOR") ENABLE
   )
/


CREATE OR REPLACE TRIGGER  "BI_SERVIDOR_TELEFONE"
  before insert on "SERVIDOR_TELEFONE"               
  for each row  
begin   
    select "SERVIDOR_TELEFONE_SEQ".nextval into :NEW.ID_SERVIDOR_TELEFONE from dual;
end;

/
ALTER TRIGGER  "BI_SERVIDOR_TELEFONE" ENABLE
/


CREATE TABLE  "UNIDADE"
   (	"ID_UNIDADE" NUMBER NOT NULL ENABLE,
	"NOME_UNIDADE" VARCHAR2(255) NOT NULL ENABLE,
	"TIPO_UNIDADE" VARCHAR2(100) NOT NULL ENABLE,
	"NUMERO_CNES" VARCHAR2(100),
	"RUA_UNIDADE" VARCHAR2(150) NOT NULL ENABLE,
	"NUMERO_UNIDADE" NUMBER NOT NULL ENABLE,
	"BAIRRO_UNIDADE" VARCHAR2(150) NOT NULL ENABLE,
	"CEP_UNIDADE" CHAR(8) NOT NULL ENABLE,
	"CIDADE_UNIDADE" VARCHAR2(150) NOT NULL ENABLE,
	 CONSTRAINT "UNIDADE_PK" PRIMARY KEY ("ID_UNIDADE") ENABLE
   )
/


CREATE OR REPLACE TRIGGER  "BI_UNIDADE"
  before insert on "UNIDADE"               
  for each row  
begin   
    select "UNIDADE_SEQ".nextval into :NEW.ID_UNIDADE from dual;
end;

/
ALTER TRIGGER  "BI_UNIDADE" ENABLE
/


CREATE TABLE  "TURMA_DATA"
   (	"ID_TURMA_DATA" NUMBER NOT NULL ENABLE,
	"DATA_AULAS" DATE NOT NULL ENABLE,
	"ID_TURMA" NUMBER,
	 CONSTRAINT "TURMA_DATA_PK" PRIMARY KEY ("ID_TURMA_DATA") ENABLE,
	 CONSTRAINT "TURMA_DATA_FK" FOREIGN KEY ("ID_TURMA")
	  REFERENCES  "TURMA" ("ID_TURMA") ENABLE
   )
/


CREATE OR REPLACE TRIGGER  "BI_TURMA_DATA"
  before insert on "TURMA_DATA"               
  for each row  
begin   
    select "TURMA_DATA_SEQ".nextval into :NEW.ID_TURMA_DATA from dual;
end;

/
ALTER TRIGGER  "BI_TURMA_DATA" ENABLE
/


CREATE TABLE  "TRABALHA"
   (	"ID_TRABALHA" NUMBER NOT NULL ENABLE,
	"ID_SERVIDOR" NUMBER,
	"ID_UNIDADE" NUMBER,
	 CONSTRAINT "TRABALHA_PK" PRIMARY KEY ("ID_TRABALHA") ENABLE,
	 CONSTRAINT "TRABALHA_FK" FOREIGN KEY ("ID_SERVIDOR")
	  REFERENCES  "SERVIDOR" ("ID_SERVIDOR") ENABLE,
	 CONSTRAINT "TRABALHA_FK2" FOREIGN KEY ("ID_UNIDADE")
	  REFERENCES  "UNIDADE" ("ID_UNIDADE") ENABLE
   )
/


CREATE OR REPLACE TRIGGER  "BI_TRABALHA"
  before insert on "TRABALHA"               
  for each row  
begin   
    select "TRABALHA_SEQ".nextval into :NEW.ID_TRABALHA from dual;
end;

/
ALTER TRIGGER  "BI_TRABALHA" ENABLE
/


CREATE TABLE  "PROFESSOR"
   (	"ID_PROFESSOR" NUMBER NOT NULL ENABLE,
	"FORMACAO_PROFESSOR" VARCHAR2(255) NOT NULL ENABLE,
	"ID_SERVIDOR" NUMBER,
	 CONSTRAINT "PROFESSOR_PK" PRIMARY KEY ("ID_PROFESSOR") ENABLE,
	 CONSTRAINT "PROFESSOR_FK" FOREIGN KEY ("ID_SERVIDOR")
	  REFERENCES  "SERVIDOR" ("ID_SERVIDOR") ENABLE
   )
/


CREATE OR REPLACE TRIGGER  "BI_PROFESSOR"
  before insert on "PROFESSOR"               
  for each row  
begin   
    select "PROFESSOR_SEQ".nextval into :NEW.ID_PROFESSOR from dual;
end;

/
ALTER TRIGGER  "BI_PROFESSOR" ENABLE
/


CREATE TABLE  "ALUNO"
   (	"ID_ALUNO" NUMBER NOT NULL ENABLE,
	"GRAU_INSTRUCAO_ALUNO" VARCHAR2(50),
	"ID_SERVIDOR" NUMBER,
	 CONSTRAINT "ALUNO_PK" PRIMARY KEY ("ID_ALUNO") ENABLE,
	 CONSTRAINT "ALUNO_FK" FOREIGN KEY ("ID_SERVIDOR")
	  REFERENCES  "SERVIDOR" ("ID_SERVIDOR") ENABLE
   )
/


CREATE OR REPLACE TRIGGER  "BI_ALUNO"
  before insert on "ALUNO"               
  for each row  
begin   
    select "ALUNO_SEQ".nextval into :NEW.ID_ALUNO from dual;
end;

/
ALTER TRIGGER  "BI_ALUNO" ENABLE
/


CREATE TABLE  "CURSO_CARGO"
   (	"ID_CURSO_CARGO" NUMBER NOT NULL ENABLE,
	"CARGOS_ACEITAVEIS" VARCHAR2(255),
	"ID_CURSO" NUMBER,
	 CONSTRAINT "CURSO_CARGO_PK" PRIMARY KEY ("ID_CURSO_CARGO") ENABLE,
	 CONSTRAINT "CURSO_CARGO_FK" FOREIGN KEY ("ID_CURSO")
	  REFERENCES  "CURSO" ("ID_CURSO") ENABLE
   )
/


CREATE OR REPLACE TRIGGER  "BI_CURSO_CARGO"
  before insert on "CURSO_CARGO"               
  for each row  
begin   
    select "CURSO_CARGO_SEQ".nextval into :NEW.ID_CURSO_CARGO from dual;
end;

/
ALTER TRIGGER  "BI_CURSO_CARGO" ENABLE
/


CREATE TABLE  "COORDENACAO"
   (	"ID_COORDENACAO" NUMBER NOT NULL ENABLE,
	"NOME_COORDENACAO" VARCHAR2(255) NOT NULL ENABLE,
	"RUA_COORDENACAO" VARCHAR2(150),
	"NUMERO_COORDENACAO" NUMBER NOT NULL ENABLE,
	"BAIRRO_COORDENACAO" VARCHAR2(150),
	"CEP_COORDENACAO" CHAR(8) NOT NULL ENABLE,
	"CIDADE_COORDENACAO" VARCHAR2(150) NOT NULL ENABLE,
	 CONSTRAINT "COORDENACAO_PK" PRIMARY KEY ("ID_COORDENACAO") ENABLE
   )
/


CREATE OR REPLACE TRIGGER  "BI_COORDENACAO"
  before insert on "COORDENACAO"               
  for each row  
begin   
    select "COORDENACAO_SEQ".nextval into :NEW.ID_COORDENACAO from dual;
end;

/
ALTER TRIGGER  "BI_COORDENACAO" ENABLE
/


CREATE TABLE  "SUPERVISAO"
   (	"ID_SUPERVISAO" NUMBER NOT NULL ENABLE,
	"NOME_SUPERVISAO" VARCHAR2(255) NOT NULL ENABLE,
	"RUA_SUPERVISAO" VARCHAR2(150) NOT NULL ENABLE,
	"NUMERO_SUPERVISAO" NUMBER NOT NULL ENABLE,
	"BAIRRO_SUPERVISAO" VARCHAR2(150) NOT NULL ENABLE,
	"CEP_SUPERVISAO" CHAR(8) NOT NULL ENABLE,
	"CIDADE_SUPERVISAO" VARCHAR2(150) NOT NULL ENABLE,
	"ID_UNIDADE" NUMBER,
	 CONSTRAINT "SUPERVISAO_PK" PRIMARY KEY ("ID_SUPERVISAO") ENABLE,
	 CONSTRAINT "SUPERVISAO_FK" FOREIGN KEY ("ID_UNIDADE")
	  REFERENCES  "UNIDADE" ("ID_UNIDADE") ENABLE
   )
/


CREATE OR REPLACE TRIGGER  "BI_SUPERVISAO"
  before insert on "SUPERVISAO"               
  for each row  
begin   
    select "SUPERVISAO_SEQ".nextval into :NEW.ID_SUPERVISAO from dual;
end;

/
ALTER TRIGGER  "BI_SUPERVISAO" ENABLE
/


CREATE TABLE  "TURMA"
   (	"ID_TURMA" NUMBER NOT NULL ENABLE,
	"QUANT_VAGAS" NUMBER NOT NULL ENABLE,
	"QUANT_AULAS" NUMBER,
	"ID_ALUNO" NUMBER,
	"ID_PROFESSOR" NUMBER,
	"ID_UNIDADE" NUMBER,
	"ID_CURSO" NUMBER,
	 CONSTRAINT "TURMA_PK" PRIMARY KEY ("ID_TURMA") ENABLE,
	 CONSTRAINT "TURMA_FK" FOREIGN KEY ("ID_ALUNO")
	  REFERENCES  "ALUNO" ("ID_ALUNO") ENABLE,
	 CONSTRAINT "TURMA_FK2" FOREIGN KEY ("ID_PROFESSOR")
	  REFERENCES  "PROFESSOR" ("ID_PROFESSOR") ENABLE,
	 CONSTRAINT "TURMA_FK3" FOREIGN KEY ("ID_UNIDADE")
	  REFERENCES  "UNIDADE" ("ID_UNIDADE") ENABLE,
	 CONSTRAINT "TURMA_FK4" FOREIGN KEY ("ID_CURSO")
	  REFERENCES  "CURSO" ("ID_CURSO") ENABLE
   )
/


CREATE OR REPLACE TRIGGER  "BI_TURMA"
  before insert on "TURMA"               
  for each row  
begin   
    select "TURMA_SEQ".nextval into :NEW.ID_TURMA from dual;
end;

/
ALTER TRIGGER  "BI_TURMA" ENABLE
/
